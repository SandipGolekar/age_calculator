import React from "react";
import styles from '../../styles/Home.module.css'

const AgeCalculator = () => {
  return (
    <div>
      <nav className="navbar navbar-dark bg-light justify-content-center">
        <h1 className="text-dark">Age Calculator</h1>
      </nav>
    </div>
  );
};

export default AgeCalculator;
